# Browser: same-origin API proxy. Do not use this for server-side fetches.
NEXT_PUBLIC_API_URL=/api
# Server (SSR + API proxy): absolute URL to the Express API on the VPS.
API_URL=http://127.0.0.1:4000/api
